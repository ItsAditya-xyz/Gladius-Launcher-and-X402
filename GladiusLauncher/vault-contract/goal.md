so we want one contract to create a receiving address where anyone can send AVAX/ERC 20 to it. 

can be many receing address as we want. no limits.

a function called transferFromBank(receivingAddres, toAnyWallet) // to be only run by the contract creator or ADMIN address as set

admin address will be contract cretor or anyone whom creators sets/removes.

